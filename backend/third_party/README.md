这个目录存放第三方库。

1. nlohmann/json (通过CMake的FetchContent获取)
2. cpp-httplib (HTTP服务器)
3. stb_image (图像处理)
4. 或者使用系统包管理器安装：
   - Ubuntu: sudo apt-get install libcurl4-openssl-dev libjsoncpp-dev
   - Windows: 使用vcpkg